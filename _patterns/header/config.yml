title: "Header"
description: |
  This is a list of all the styles for the header of the website.

patterns:
  header:
    title: "Site Header"
    description: |
      This is the header of the website.
    fields:
      - name: test
        type: string
        example: "TEEST"
